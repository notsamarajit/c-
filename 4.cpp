#include <bits/stdc++.h>
using namespace std;

int main (){
    // while loop :
    int n,i;
    cout << "enter value of n :";
    cin >> n;
    i = 1; // this is declaration of starting or initial element;
    while (i <=n)  // this is the codition 
    {
        cout << i << endl;  // the command we want to execute until condition is satisfied;
        i = i +1; // the step we want to take after  excution 
    }
    return 0;


} 
